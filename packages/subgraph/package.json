{
  "name": "microloan-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ microloan",
    "create-local": "graph create --node http://localhost:8020/ microloan",
    "remove-local": "graph remove --node http://localhost:8020/ microloan",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 microloan"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}